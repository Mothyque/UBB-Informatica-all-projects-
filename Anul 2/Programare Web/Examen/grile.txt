- Un apel AJAX se poate face catre o resursa statica oferita de end-point-ul de pe server?
a. Doar daca in QUERY_STRING / body-ul POST-ului se trimit spre server si parametrii
b. Da, intotdeauna
c. Nu, niciodata

- #RRAABB este un cod de culoare valid?
a. Nu, deoarece codul de culoare hexazecimal trebuie sa contina inca 2 cifre pentru opacitate
b. Da, deoarece valorile sunt specificate in baza 16
c. Da, deoarece RR specifica cantitatea de rosu, AA cantitatea de verde, BB cantitatea de albastru 
d. Nu, deoarece valorile nu sunt specificate in baza 16

- Un formular care contine un input de tip file trebuie:
a. Submis prin metoda GET
b. Submis prin metoda POST
c. Sa aiba specificat atributul accept care specifica tipul fisierelor ce se pot trimite la server
d. Sa aiba specificat atributul enctype setat la multipart/form-data
e. Sa aiba specificat atributul content care sa indice spre continutul fisierului 

- Care dintre urmatoarele afirmatii referitoare la filtre de vizibilitate sunt adevarate?
a. Selectorul :hidden se foloseste pentru a selecta toate elementele ascunse in pagina
b. Selectorul :visible se foloseste pentru a selecta toate elementele care un display:none sau width/height:0
c. Selectorul :hidden se foloseste pentru a selecta toate elementele care ocupa spatiu in pagina
d. Selectorul :visible se foloseste pentru a selecta toate elementele care ocupa spatiu in pagina
e. Selectorul :visible se foloseste pentru a selecta elementele care au visibility:hidden si opacity:0

- Ce indica un cod de raspuns de forma 3xx trimis prin intermediul protocolului HTTP de un server web?
a. Mutarea documentului cerut la o alta adresa
b. O eroare efectuata de client
c. Redirecteaza clientul spre alt URL
d. O eroare aparuta pe partea de server

- De pe client se doreste salvarea unor date despre o persoana (numele si varsta acesteia) folosind un apel AJAX realizat prin metoda HTTP GET. Care dintre urmatoarele apeluri realizeaza acest lucru?
a. $.get("save.php", {nume: 'Ion', varsta: 10})
b. $.get("save.php?nume=Ion&varsta=10")
c. $.get("save.php", 'nume=Ion&varsta=10')

- Un URL de pe back-end poate fi invocat atat prin GET cat si prin POST:
a. Nu
b. Da
c. Doar daca datele primite fie prin GET fie prin POST sunt aceleasi

- Un fisier PHP poate fi executat:
a. In linia de comanda folosind interpretorul php.exe pe Windows sau php pe Linux
b. De catre browser
c. De catre interpretorul de comenzi al sistemului de operare pe care este instalata stiva AMP
d. De catre un modul din cadrul serverului web

- Cum se declara un array vid in PHP:
a. $emptyArray = [];
b. $emptyArray = array();
c. $emptyArray = null;
d. $emptyArray = {};

- Ce face urmatoarea secventa de cod

$(':not(p)').addClass('patrat).addClass('colorat);

a. Selecteaza toate elementele cu exceptia paragrafelor si le adauga clasele patrat si colorat
b. Selecteaza toate elementele care au clasa patrat si clasa colorat cu exceptia paragrafelor
c. Selecteaza toate elementele cu exceptia paragrafelor care au clasa patrat si clasa colorat
d. Selecteaza toate paragrafele care au clasa patrat si clasa colorat

- Pentru a defini o lista ordonata a caror itemi sunt precedati de litere din alfabetul grec in CSS se foloseste:
a. List-type:lower-greek
b. List-style-type:lower-greek
c. List:lower-greek

- Pentru a redirectiona automat browser-ul spre un nou URL, serverul poate:
a. Raspunde clientului cu un cod de raspuns de forma 3xx impreuna cu un header location
b. Trimite ca si continut HTML clientului un link de forma <a href="http://url-nou"> si simuleaza din JavaScript un click de mouse pe link
c. Include noul URL direct pe back-end folosind o directiva include
d. Redirectioneaza clientul folosind o secventa de cod in JavaScript de forma window.location = "http://url-nou"

- Care dintre urmatoarele tag-uri HTML sunt deprecated in HTML5?
a. center
b. font
c. b
d. body

- Cum se insereaza corect un fisier extern JavaScript denumit test.js intr-un document HTML?
a. <script name = "test.js"></script>
b. <script src = "test.js">
c. <script href = "test.js">
d. <script src = "test.js"></script>

- Pentru inserarea de diacritice intr-un document HTML se poate folosi:
a. Editoare capabile sa salveze fisierul UTF-8 si specificarea acestui set de caractere in sectiunea head a documentului HTML
b. Entitati HTML
c. Tastatura setata pe limba romana si specificarea atributului lang="ro-RO" la tag-ul html

- Care sunt asemanarile si diferentele dintre atributele id si name?
a. Atributul name se foloseste pentru referirea unui element din CSS/JavaScript
b. Doua elemente HTML pot avea acelasi nume, dar nu pot avea acelasi id
c. Orice tag accepta atributul id, nu toate tagurile accepta atributul name
d. Orice tag accepta atributul name, nu toate tagurile accepta atributul id
e. Doua elemente HTML pot avea acelasi id, dar nu pot avea acelasi nume
f. Atributul id se foloseste pentru referirea unui element din CSS/JavaScript

- In JavaScript typeof 1/0 este NaN pentru ca:
a. Type of 1/0 nu are rezultatul NaN, ci Infinity
b. Type of 1 este Number si Number nu se poate imparti la 0, deci rezultatul este NaN
c. 1/0 este egal cu Infinity si typeof Infinity este NaN
d. 1/0 este NaN si typeof NaN este NaN

- Ce se intelege prin scop global in JavaScript?
a. Obiectul window
b. Obiectul document
c. Variabila this
d. Scopul imediat exterior celui in care este declarata functia

- Aranjat in ordine, de la cea mai simpla si putin costumizabila la cea mai configurabila si parametrizabila urmatoarele functii care permit realizarea unui apel AJAX din jQuery:
a. $get, load, $.post
b. load, $.get, $.ajax
c. $post, load, $.ajax
d. $.ajax, load, $.get

- Care este forma corecta de folosire a tag-ului img in cadrul unui document XHTML:
a. <img> poza.jpg </img>
b. <img src="poza.jpg" />
c. <img src="poza.jpg"> </img>
d. <img src="poza.jpg">

- Care dintre urmatoarele reprezinta masuri pentru evitarea injectiilor SQL:
a. Verificari riguroase la nivelul backend-ului legate de validitatea datelor introduse precum si folosirea de biblioteci specializate pentru persistarea datelor (ORM-uri)
b. Dezactivarea in cadrul aplicatie Web a posibilitatii rularii de cod SQL direct de catre browser
c. Verificari riguroase la nivelul backend-ului legate de validitatea datelor introduse
d. Folosirea la nivelul backend-ului de mecanisme de tipul "prepared statement"

- Ce face urmatoarea secventa de cod:

var content = $('li').html();
$('li').append('<em> ' + content + '</em>');

a. Introduce continutul primului list item inaintea fiecarui list item
b. Continutul din toate li-urile vor fi scrise cu italica
c. Introduce content dupa primul list item
d. Introduce continutul primul list item dupa fiecare list item
e. Continutul din primul list item va fi scris cu italic

- Care dintre urmatoarele afirmatii despre metoda HTTP POST sunt adevarate?
a. Trebuie neaparat folosita pentru a submite un formular care contine un input de tip video
b. Este recomandat a fi folosita pentru a submite un formulare care contine un input de tip password
c. Trebuie neaparat folosita pentru a submite un formular care contine un input de tip file

- Fie doua variabile $a si $b in PHP ce contin siruri de caractere. Aceste doua siruri in PHP se pot concatena folosind:
a. $s = $a + $b;
b. $s = $a . $b;
c. $s = "$a$b";
d. $s = '$a$b';

- Pozitionarea relativa este folosita pentru:
a. A pozitiona / muta un element in direcite directii relativ la pozitia sa normala
b. A pozitiona un element relativa la obiectul document
c. A pozitiona un element relativ la fereastra browser-ului
d. Un container parinte care are elemente fiu pozitionate absolut

- Care dintre urmatoarele metode HTTP nu presupun trimiterea de continut dupa antete in cadrul unei cereri HTTP:
a. GET
b. POST
c. PUT
d. HEAD

- Considerand urmatoarele elemente html:

<div class="info">John Doe</div>
<div class="info number" id="age">25</div>

Ce selector CSS ar selecta doar cel de al doilea div?
a. .info.number
b. .info .number
c. .info #age
d. .info[id]

- In ce conditii metoda send pe un obiect AJAX se apeleaza intotdeauna cu parametrul sirul vid?
a. Daca requestul AJAX se face prin GET
b. Daca requestul AJAX se face prin HTTP
c. Metoda send nu se poate apela cu un parametru sirul vid

- Cum se ascund toate div-urile din pagina folosind jQuery?
a. $("div").hidden();
b. $("div").visibility(false)
b. $("div").css("visibility", "hidden");
b. $("div").css("visibility", "hide");

- Care dintre urmatoarele afirmatii sunt adevarate:
a. MySQL Old Extension (mysql_*) nu suporta "prepared statements"-uri, dar permite evitarea injectiilor SQL
b. MySQL ImprovedExtension(mysqli_*) asigura in plus fata de PDO independenta fata de sistemul de gestiune al bazelor de date folosit
c. MySQL ImprovedExtension(mysqli_*) asigura in plus fata de PDO posibilitatea folosirii de "prepared statements"-uri

- In PHP return este folosit pentru:
a. A termina scriptul curent care se executa si a reda controlul executiei unui eventual script care l-a inclus pe scriptul curent cu include sau require
b. A termina executia logicii ce se executa pe server si a trimite raspunsul complet clientului
c. A trimite clientului un anumit raspuns in urma cererii facute de acesta
d. A termina executia unei functii

- Pe ce nivel al stivei TCP/IP se afla protocolul HTTP?
a. Aplicatie
b. Internet
c. Legatura de date
d. Transport

- In jQuery $(this) se foloseste pentru:
a. A construi un wrapper jQuery in jurul obiectului pe care se apeleaza un eveniment in interiorul functiei de tratare a evenimentului
b. Pentru a construi un wrapper jQuery in jurul documentului
c. A returna obiectul JavaScript de baza in jurul caruia este construit obiectul jQuery curent
d. Pentru a construi un wrapper jQuery in jurul obiectului window

- Un apel AJAX este in starea 2 daca:
a. S-a facut send dar inca nu a sosit raspunsul de la server
b. S-a facut receive, dar nu s-a facut send
c. S-a facut si send si receive, chiar inainte de close
d. S-a facut open dar inca nu s-a facut send

-Elementul <th> trebuie folosit in cadrul unui tabel pentru:
a. Centrarea textului si bold-area continutului unei celule
b. Definirea unei celule avand semnificatia de celula ce face parte din header-ul tabelului
c. Definirea unui nou rand in tabel

- Cum se declara corect un array in JavaScript?
a. var studenti = {"Mihai", "Cristina", "Paula", "Dan"}
b. var studenti = ("Mihai", "Cristina", "Paula", "Dan")
c. var studenti = "Mihai", "Cristina", "Paula", "Dan"
d. var studenti = ["Mihai", "Cristina", "Paula", "Dan"]

- Pentru evitarea injectiilor SQL in PHP se recomanda:
a. Folosirea de "prepared statement"-uri
b. Evitarea acestora folosind mysql_real_escape_string
c. Eliminarea ghilimelelor si apostroafelor din datele primite de la client

- Injectiile JavaScript se datoreaza:
a. Folosirii protocolului http in locul protocolului https
b. Validarii insuficiente chiar la nivelul codului JavaScript
c. Unor buguri prezente la nivelul browserului web
d. Validarii insuficiente server-side la nivelul scriptului ce prelucreaza datele din formular

- Unde se poate defini un stil CSS pentru o anumita clasa?
a. In interiorul tag-ului <style>, localizat in antetul paginii html
b. Inline, cu ajutorul atributului style aplicat pe un tag html
c. Intr-un fisier extern specificat cu ajutorul tag-ului <link>

- Pentru a verifica succesul unui apel AJAX trebuie ca:
a. readyState sa fie 4 si request.status sa fie 200
b. readyState sa fie 4 si request.status sa fie 100
c. readyState sa fie 1 si request.status sa fie 200
d. readyState sa fie 0 si request.status sa fie 100

- Care dintre urmatoarele elemente au toate display-ul implicit block?
a. ul, ol, p, img, div
b. ul, ol, p, table, input
c. input, div, p, form
d. div, p, ul, ol, h1

- Unde se poate insera cod JavaScript in cadrul unui document HTML?
a. Doar in sectiunea <body>
b. Doar in sectiunea <head>
c. Atat in sectiunea <head> cat si in sectiunea <body>

- Fie urmatoarea secventa de cod HTML/CSS. Pe ce fundal va fi afisat textul "Cocosul canta"?

style type="text/css">
    #id2 {
        background-color: blue;
    }

    div #id1 .class2 {
        background-color: green;
    }

    div .class1 #id2 {
        background-color: yellow;
    }

    #id1 {
        background-color: red;
    }
</style>
<div> 
    <div class="class1" id="id1">
        <div class="class2" id="id2">
            Cocosul canta
        </div>
    </div>
</div>

a. Rosu
b. Verde
c. Galben
d. Albastru

- Care dintre urmatoarele functii PHP este folosita pentru a preveni vulnerabilitatile de tip XSS?
a. mysql_real_escape_string
b. htmlentities
c. filtext_XSS

- Care dintre urmatoarele vulnerabilitati ar putea fi exploatata pentru a fura sesiunea unui utilizator autentificat?
a. CSRF
b. SQL Injection
c. XSS

- Ce se va afisa in consola browserului dupa executarea urmatoarei secvente de cod JavaScript:

if (1 === '1') {
    consol.log("1 === '1'");
} else if (1 == true) {
    console.log("1 == true");
} else if (1 === 1.0) {
    console.log("1 === 1.0");
}

a. nimic
b. 1 == 1.0
c. 1 == true
d. 1 === '1'

- Care dintre urmatoarele categorii de aplicatii se pot folosi de DOCTYPE pentru a "intelege" mai bine documentul HTML
a. Servere web
b. Editoare si medii IDE
c. Motoare de cautare
d. Browsere 

- Care dintre informatiile de mai jos despre atributele readonly si disabled sunt adevarate?
a. Valoarea unui input readonly se trimite serverului la submit , pe cand cea a unui input disabled nu se trimite
b. Valoarea unui input readonly poate fi modificata din JavaScript, pe cand cea a unui input disable nu poate fi editata nici din JavaScript
c. Atributeel specifica acelasi lucru, faptul ca un input readonly sau disable nu poate fi editat

- Care dintre urmatoarele elemente au toate display-ul implicit inline?
a. a, span, b, i, li
b. img, span, b, i, a
c. p, a, b, i, span
d. b, i, li, span, img

- Care dintre urmatoarele afirmatii despre tagul <title> sunt adevarate?
a. Este folosit in mare masura de catre motoarele de cautare
b. Este optional
c. Este afisat in bara de titlu sau de bookmarks (favorite)
d. Nu exista tag-ul title ci doar atributul title

- Care dintre urmatoarele antete sunt obligatorii pentru o cerere HTTP in cazul folosirii protocolului HTTP/1.1:
a. Content-Type
b. Host
c. User-Agent
d. Cookie

- Ce face urmatoarea secventa de cod:

$('li.first').addClass('patrat').addClass('deplasat').addClass('colorat');

a. Selecteaza primul li si ii adauga clasele patrat, deplasat si colorat
b. Selecteaza toate listele si le adauga clasele patrat, deplasat si colorat
c. Selecteaza toate elementele din liste si le adauga clasele patrat, deplasat si colorat
d. Selecteaza primul element din fiecare lista si le adauga clasele patrat, deplasat si colorat

- Care dintre urmatoarele afirmatii sunt adevarate:
a. Definitiile de stil interne au prioritate fata de cele externe
b. O definitie de stil aplicata clasei "bate" ca prioritate definitia de stil aplicata pe "tag"
c. O proprietate din cadrul unui stil inline suprascrie o proprietate dintr-o definitie de stil aplicata elementului folosind un selector bazat pe id 

- Ce face functia session_start() in PHP?
a. Daca este vorba de o sesiune noua, genereaza un cookie aleator de sesiune pe care il trimite folosind antetul HTTP Cookie clientului
b. Initializeaza intotdeauna o noua sesiune
c. Porneste sesiunea existenta deja si trimite clientului cookie-ul de sesiune prin intermediul antetului HTTP Cookie
d. Daca este vorba de o sesiune veche, determina despre ce sesiune este vorba pe baza valorii cookie-ului de sesiune prin intermediul antetului HTTP Cookie si populeaza tabloul $_SESSION in mod corespunzator

- Care dintre urmatoarele declaratii este corecta pentru definirea tabloului $fructe in PHP?
a. $fructe = array("struguri", "mere", "pere");
b. $fructe = ("struguri", "mere", "pere");
c. $fructe = "struguri", "mere", "pere";
d. $fructe = {"struguri", "mere", "pere"};
e. $fructe = ["struguri", "mere", "pere"];

- Unde se poate folosi functia header in PHP?
a. In primul scriptlet din fisierul PHP si doar cu conditia ca scriptul PHP sa nu fi trimis anterior spre browser orice forma de continut (cu exceptia unor eventuale antete)
b. In cadrul unui scriptlet PHP plasat in sectiunea documentului HTML, generat de fisierul PHP
c. In orice scriptlet din cadrul fisierului PHP

- Care dintre urmatoarele informatii despre un URL, accesat prin POST sunt adevarate:
a. Reaccesarea URl-ului respectiv prin reload (refresh) la pagina presupune o confirmare din partea utilizatorului
b. URL-ul respectiv i se poate face bookmark 
c. URL respectiv poate fi partajat pe Facebook, Whatsapp, Skype, etc.

- Care dintre urmatoarele sunt adevarate despre un server web:
a. Un server web poate fi configurat sa accepte cereri prin protocolul HTTP pe portul 443
b. Un server web poate fi configurat sa accepte cereri printr-un URL de forma file://
c. Implicit un server web asteapta cereri prin protocolul HTTP pe portul 80 si cereri prin protocolul HTTPS pe portul 443

- Dandu-se doua containere, unul parinte si unul fiu, care dintre urmatoarele pozitionari au sens:
a. Pozitionarea unui fiu "absolute" in cadrul unui parinte cu pozitionare "relative"
b. Pozitionarea unui fiu "relative" intr-un parinte cu pozitionare "virtual"
c. Pozitionarea unui fiu "flex" intr-un parinte "relative"
d. Pozitionarea unui fiu "fixed" intr-un parinte cu pozitionare "relative"

- Cum se introduc comentarii intr-un scriptlet PHP?
a. //
b. <!-- -->
c. /* ... */
d. #

- Functia specificata ca si valoare pentru proprietatea onreadystatechange se apeleaza:
a. Este posibil sa se apeleze si atunci cand obiectul nu isi schimba starea dar continua sa vina raspuns de la server
b. Atunci cand serverul schimba codul de raspuns (response code-ul) care e trimis clientului
c. Atunci cand obiectul AJAX isi schimba starea

- Conectarea la o baza de date dintr-o aplicatie web Java se recomanda a fi facuta:
a. La initializarea aplicatiei web / a contextului
b. La fiecare request in cadrul metodei doGet sau doPut
c. La pornirea containerului, o singura conexiune pentru toate aplicatiile web (din motive de performanta)

- Care dintre urmatoarele expresii jQuery se pot folosi interschimbabil?
a. $(ready(function() {})) cu $(document).ready(function() {})
b. $(function() {}) cu $(document).ready(function() {})
c. $(selector).each(function() {}) cu $(selector.each).function() {}

- Care dintre urmatoarele reprezinta masuri pentru evitarea vulnerabilitatilor de tip XSS:
a. Verificari riguroase la nivelul browserului legate de validitatea datelor introduse
b. Folosirea la nivelul browserului a unor biblioteci de functii JavaScript consacrate si testate anterior
c. Inlocuirea anumitor caractere din datele primite de la client cu entitatile HTML corespunzatoare
d. Dezactivarea din cadrul aplicatiei web a posibilitatii rularii de cod JavaScript de catre browser

- Care dintre urmatoarele tag-uri este folosit pentru definirea de stiluri CSS interne
a. Script
b. CSS
c. Style
d. Class

- Care dintre sintaxele CSS de mai jos sunt corecte?
a. body:color=red
b. {body:color=red}
c. body{color.#abc}
d. {body;color:blue}
e. body{color:yellow}

- O baza de date embedded in cadrul unei aplicatii web Java se recomanda a fi memorata:
a. In cadrul folderului app/databases, unde app reprezinta directorul aplicatiei web
b. In folderul WEB-INF al aplicatiei web intrucat acesta nu este accesibil de catre un client web
c. Oriune altundeva in cadrul sistemului de fisiere dar nu in cadrul directorului aplicatiei web, intrucat de acolo baza de date ar putea fi accesata de catre clientii web

- In care element HTML se plaseaza codul JavaScript?
a. <javascript>
b. <js>
c. <script>

- In cate instante se instantiaza un servlet?
a. In niciuna, un servlet este executat direct de catre container / application server
b. Intr-o singura instanta
c. Se creaza o instanta separata la fiecare cerere facuta de catre un client

- Parametrii de configurare ai unei aplicatii web Java sunt preluati din fisierul de cofigurare ai aplicatiei web in cadrul metodei:
a. init()
b. applicationCreated()
c. contextInitialized()

- O sesiune intr-o aplicatie web Java se termina cu:
a. session.destroy()
b. session.close()
c. session.invalidate()

- Ce eveniment se genereaza la un click de mouse pe un element din cadrul documentului?
a. onClick
b. onDoubleClick
c. onmouseover
d. onmouseclick

- Care dintre urmatoarele expresii jQuery se pot folosi interschimbabil?
a. $("#myelem")[0] si document.getElementById("myelem")
b. $("myelem") si document.getElementById("myelem")
c. $(document.myelem) si $(document.getElementById("myelem"))
d. $ si jQuery
e. $(document).myelem si $("#myelem")[0]

- Care dintre urmatoarele reprezinta specificatii ale unor metode din API-ul jQuery?
a. Ca pot fi apelate atat in mod sincron cat si in mod asincron
b. Ca intorc referinta la obiectul pe care au fost apelate
c. Ca pot fi apelate atat pe obiectul din DOM cat si pe wrapperul jQuery construit in jurul lui
d. Ca pot fi folosite atat ca functii Setter cat si ca functii Getter

- Cum se afiseaza un mesaj prin intermediul unei ferestre de dialog modale in JavaScript?
a. modalMessage("Mesaj")
b. alertBox("Mesaj")
c. alert("Mesaj")
d. modalBox("Mesaj")

- Care dintre urmatoarele metode jQuery folosesc AJAX:
a. $.ajax, $.get, load
b. $.post, $.request, load
c. $.ajax, $.get, $.post, $.request
d. $get, load, unload

- Un client face o cerere iar serverul ii raspunde cu "404 Not found". Care dintre urmatoarele afirmatii sunt adevarate:
a. Chiar daca nu a gasit fisierul cerut de client, serverul ii poate da totusi acestuia un anumit continut HTML
b. Daca nu a gasit fisierul cerut, serverul nu ii mai poate returna clientului nimic dupa codul de raspuns 404
c. Chiar daca serverul a raspuns cu "404 not found", browserele raspund cu "200 Ok" pentru a-i spune serverului ca au primit mesajul de eroare

- Care dintre urmatoarele sunt prefixe acceptate pe unele browsere pentru proprietatile CSS experimentale?
a. -webkit-
b. -o-
c. -moz-
d. -safari-
e. -chrome-

- Care dintre urmatoarele sunt scriptlet-uri valide in PHP?
a. <% %>
b. <?php></?>
c. <?= ?>
d. <? ?>
e. <?php ?>
f. <php></php>

- Care dintre urmatoarele cuvinte/caractere nu sunt rezervate in JavaScript si se pot folosi ca si nume de variabile?
a. this, a, b
b. with, a, b
c. $, a, b
d. let, a, b

- Pentru a centra un element cu display-ul block in cadrul unui alt container se foloseste:
a. align: center
b. margin: 0 auto
c. align: middle
d. text-align: center

- Ce face urmatoarea secventa de cod: $('li:first').addClass('patrat').addClass('deplasat').addClass('colorat');
a. selecteaza toate listele si le adauga clasele patrat, deplasat, colorat
b. selecteaza primul element din fiecare lista si ii adauga clasele patrat, deplasat, colorat
c. selecteaza toate elementele din liste si le adauga clasele patrat, deplasat, colorat
d. selecteaza primul li si ii adauga clasele patrat, deplasat, colorat

- Dandu-se doua variabile $a si $b, cum se poate interschimba valoarea acestora in PHP?
a. list($a, $b) = ($b, $a);
b. array($a, $b) = ($b, $a);
c. $a = $b; $b = $a;
d. array($a, $b) = [$b, $a];
e. list($a, $b) = [$b, $a];
f. array($a, $b) = array($b, $a);
g. array($a, $b) = list($b, $a);
h. list($a, $b) = array($b, $a);
i. list($a, $b) = list($b, $a);

- Ce face urmatoarea secventa de cod: $('ul:has(li)').addClass('patrat').addClass('deplasat').addClass('colorat');
a. selecteaza toate elementele din liste si le adauga clasele patrat, deplasat, colorat
b. selecteaza listele neordonate care au cel putin un element si le adauga clasele patrat, deplasat, colorat

- Cum se poate "fura" un cookie de sesiune al unui alt utilizator?
a. Prin lipsa invalidarii sesiunii si navigarea in continuare pe un site malitios
b. Prin intermediul unui cod JavaScript injectat de catre atacator (XSS)
c. Prin interceptarea datelor la nivelul retelei de transport in lipsa folosirii unei conexiuni sigure

- Care dintre urmatoarele reprezinta proprietati membre ale unui obiect de tipul XMLHttpRequest:
a. readyState, response, responseText, status
b. state, request, response, status
c. readyState, request, response, status

- Care dintre urmatoarele informatii despre plugin-urile jQuery sunt adevarate:
a. Unele plugin-uri permit portarea codului jQuery pe diferite browsere
b. Permit extinderea functionalitatii API-ului standard jQuery cu noi metode si functionalitati
c. Unele plugin-uri jQuery permit incadrarea librariei de pe diversele CDN-uri
d. Unele plugin-uri permit rulearea codului jQuery independent

- Raspunsul sosit printr-un apel AJAX poate fi:
a. un fisier text
b. un fisier JSON
c. un fisier XML
d. un fisier JavaScript